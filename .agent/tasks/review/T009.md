# T009: 按键展示位置

## 元信息

- 优先级：P1
- 状态：REVIEW
- 推荐执行器：Sol
- 依赖：无

## Completion Report

- `KeycastSettings` 新增九宫格 `KeycastOverlayPosition` 与归一化自定义坐标，旧配置缺省为底部预设。
- 按键浮层每次显示以鼠标所在屏幕定位；自定义拖动位置按可见区域比例保存并在屏幕变化时收敛。
- 设置页提供九宫格预设和自定义拖动入口。
- 验证：FewerCore 256 tests passed；FewerShortcutHelper Debug build succeeded；完整 Fewer Debug build succeeded。

## Remaining Acceptance

- 需在多显示器实机上验证负坐标副屏、跨屏鼠标、拖动持久化和屏幕移除后的展示位置。
