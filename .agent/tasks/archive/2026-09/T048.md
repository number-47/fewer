# T048: 补齐快捷键设置入口的一致性

## Metadata

- Priority: P0
- Status: DONE
- Dependencies: T042

## Goal

确保所有现有截图快捷键设置入口均显示并保护“截图翻译”快捷键，避免用户在第二个设置页无意配置冲突。

## Scope

- 仅修改 `FewerApp/Views/ShortcutSettingsView.swift` 与 `FewerApp/Views/InputEnhancementSettingsView.swift`。
- 复用既有 `ocrTranslateHotKey`；不修改热键注册、截图/OCR 流程、设置持久化或任何用户已有脏文件。

## Acceptance Criteria

- “快捷键”设置页的截图快捷键卡片显示“截图翻译”录制行。
- 输入增强的 Keycast 冲突预留列表包含该快捷键。
- 不改变其他快捷键显示或存储行为。

## Validation

- Fewer Debug 无签名构建。
- `git diff --check`。
- 签名 UI 实测由 T047 一并覆盖。

## Evidence

- 2026-09-02：签名 Debug 运行后的当前源码核对发现 `ShortcutSettingsView` 和 `InputEnhancementSettingsView` 漏掉了 T042 新字段；两个目标文件在任务开始前无既有脏改动。
- 2026-09-02：快捷键设置页新增“截图翻译”录制行；输入增强的 Keycast 冲突预留数组纳入 `ocrTranslateHotKey`。主对话验收仅含两处目标修改，未扩大范围。
- 2026-09-02：Fewer Debug 无签名构建成功；`git diff --check` 通过。签名 UI 实测仍与 T047 的完整运行时矩阵合并执行。
