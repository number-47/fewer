# T042: 截图翻译设置与快捷键配置

## Metadata

- Priority: P0
- Status: DONE
- Dependencies: 无

## Goal

为截图翻译增加独立、可录制且与旧配置兼容的快捷键设置；默认值为 Command-Option-T。

## Scope

- 仅修改 `FewerCore/Models/ScreenshotSettings.swift`、其 XCTest，以及 `FewerApp/Views/ScreenshotSettingsView.swift`。
- 新字段必须以 `decodeIfPresent` 回退默认值，保留所有既有截图快捷键和 Keycast 冲突保护。
- 不注册新热键、不修改截图服务或模块菜单；这些由后续任务完成。

## Acceptance Criteria

- `ScreenshotSettings` 提供 `ocrTranslateHotKey`，默认值为 Command-Option-T，并完成 Codable 编解码。
- 缺少该字段的旧 JSON 可解码且得到默认快捷键。
- 截图设置页提供“截图翻译”录制行。
- Keycast 全局快捷键冲突预留集合包含该新增快捷键。

## Validation

- 对旧 JSON 兼容和默认快捷键的 XCTest。
- `xcodebuild -project Fewer.xcodeproj -scheme FewerCore -configuration Debug -derivedDataPath .build/DerivedData SYMROOT="$PWD/.build/DerivedData/Build/Products" OBJROOT="$PWD/.build/DerivedData/Build/Intermediates.noindex" CODE_SIGNING_ALLOWED=NO test | xcbeautify`
- `git diff --check`

## Evidence

- 2026-09-02：实施启动前基线：`git status --short` 显示 T041/进程关闭相关脏文件，但 T042 允许修改的三个目标文件均无既有改动；代码知识图索引连续两次崩溃，已改用定向源码检索。
- 2026-09-02：新增 `ocrTranslateHotKey`（默认 Command-Option-T），完成编码、旧 JSON 默认回退、设置录制行与 Keycast 冲突预留；修改仅限三个指定目标文件。
- 2026-09-02：主对话验收通过。`git diff --check` 通过；FewerCore 373 tests、0 failures；Fewer Debug 无签名构建成功。签名 UI/实际 Keycast 冲突交互不属于本任务，留待端到端验收。
