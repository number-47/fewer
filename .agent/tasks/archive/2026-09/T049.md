# T049: 补齐菜单栏截图翻译入口

## Metadata

- Priority: P0
- Status: DONE
- Dependencies: T046

## Goal

让菜单栏独立截图菜单与 ModuleHost 路由一致，用户可从该菜单启动截图翻译。

## Scope

- 仅修改 `FewerApp/App/FewerApp.swift` 的截图菜单与其必要高度。
- 只新增“截图翻译”行，调用已有 `execute("screenshot", "ocr-translate")`；保留该文件既有监视器 Popover 尺寸脏改动。

## Acceptance Criteria

- 截图菜单在全屏截图后显示“截图翻译”。
- 该入口复用已有 ModuleHost 路由，不建立第二套流程。
- 菜单高度能容纳新增行。

## Validation

- Fewer Debug 无签名构建。
- `git diff --check`。
- 签名 UI 菜单点击由 T047 端到端矩阵覆盖。

## Evidence

- 2026-09-02：签名 Debug 运行后的源码核对发现 `ModuleMenuPanelView.screenshot` 仍只有四个旧截图入口；该文件已有仅涉及 monitor Popover 高度的脏改动，已检查并保留。
- 2026-09-02：新增“截图翻译”行，显示当前保存快捷键并复用 `execute("screenshot", "ocr-translate")`；截图菜单首段高度从 270 调整到 300 以容纳新增行。
- 2026-09-02：Fewer Debug 无签名构建成功；仅有该文件预存的两个 `NSStatusItem.view` 弃用警告；`git diff --check` 通过。签名 UI 菜单点击留待 T047。
