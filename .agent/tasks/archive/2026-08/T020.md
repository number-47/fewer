# T020: R0 — 真实运行基线与扩展问题扫描

## 元信息

- ID：T020
- 优先级：P0
- 状态：DONE
- 推荐执行器：主对话（Sol，负责歧义拆分、真实路径取证和后续任务校准）
- 依赖：无

> 用户于 2026-08-24 决策：跳过签名实机六类问题复现验收，标记本任务完成。六类问题（右键菜单缺失、Finder 菜单缺失、权限入口、日历卡顿、设置卡顿、手势问题）的真实运行复现与证据采集留待 T012 集成验收或后续实机会话完成；本任务不产出生产代码改动。

## Goal

不修改生产代码，使用当前签名构建逐条复现用户报告的六类问题，并执行一轮相邻功能扫描。输出可重复步骤、环境、日志、事件/性能证据和第一处失败，将“原生右键菜单”“Finder 的 Fewer 菜单项”和“手势动作”明确区分。

## Rationale

当前问题跨主 App、Finder Extension、Shortcut Helper 和系统权限。单元测试已通过但实机仍失败，继续从代码假设直接修复会重复 T011 的问题。必须先固定运行版本、权限、Helper 路径和前台应用，观察第一处真实失败。

## Scope

- 当前工作树对应的签名 Debug App、嵌套 Helper、Finder Extension
- Finder、Chrome、Safari、桌面、设置窗口、菜单栏日历
- 辅助功能、输入监控、屏幕录制、日历/提醒、Finder Extension 启用状态
- 同类扫描：Helper 重启、睡眠唤醒、应用切换、多显示器、鼠标/触控板切换、菜单栏模块弹窗

## Relevant files

- `script/build_and_run.sh`
- `FewerShortcutHelper/EventTapController.swift`
- `FewerFinderExtension/FinderSync.swift`
- `FewerApp/Views/OverviewView.swift`
- `FewerApp/Views/MenuBarCalendarView.swift`
- `FewerApp/Services/ExtensionStatusService.swift`

## Requirements

1. 记录实际运行的 App/Helper bundle URL、版本、PID、签名标识和 `shortcut-helper-status.json`，排除旧构建复用。
2. 每个应用分别测试：纯右键不移动、轻微抖动 5–15pt、明确手势、未匹配手势、按住超过 3 秒、拖动中切换应用。
3. Finder 分三层记录：系统原生菜单是否出现、Fewer 菜单项是否出现、点击 Fewer 动作是否执行。
4. 记录 `pluginkit` 状态、Finder Extension 日志、模块启用状态、`menu(for:)` 是否到达及返回 nil 的原因。
5. 用 Time Profiler/sample 或等价证据测量：设置首次打开、概览停留、滑块拖动、日历连续滚动；指出主线程最重的同步调用。
6. 扫描相邻高风险路径并把新发现的问题按 P0/P1/P2 写入现有任务或新增任务，不在本任务修代码。

## Implementation guidance

- 优先使用 `./script/build_and_run.sh --logs` 和系统日志；不要通过改设置、重置权限或杀 Finder 来掩盖现象。
- 对“右键菜单不显示”保留两种解释，直到证据能区分。
- 每个结论都注明“自动化证据”或“签名实机证据”。

## Acceptance criteria

- [ ] 六类用户问题各有可重复步骤、期望/实际结果和第一处失败证据。
- [ ] 普通右键、手势右键和 Finder 扩展菜单被分别记录。
- [ ] 卡顿路径有主线程采样，不只凭肉眼判断。
- [ ] 至少覆盖 Finder/Chrome/Safari、轻微抖动、超时、应用切换、Helper 重启和权限未授予场景。
- [ ] 新发现的问题已进入任务清单或明确说明未发现新的 P0/P1。
- [ ] 未修改生产代码、系统全局设置或权限状态。

## Validation commands

```bash
./script/build_and_run.sh --logs
codesign -dv --verbose=4 .build/DerivedData/Build/Products/Debug/Fewer.app
pluginkit -m -i com.number47.fewer.finder-extension
```

## Exclusions

- 实现任何修复。
- 自动重置 TCC、强制重启 Finder 或删除用户设置。
