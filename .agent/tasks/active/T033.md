# T033: 普通 CI UI smoke

## Metadata

- Priority: P1
- Status: BLOCKED
- Dependencies: T027

## Goal

让 GitHub Actions 运行无需 TCC 的稳定 UI smoke，尽早发现设置与模块导航回归。

## Scope

- 维护可重复 UI-test 启动模式，跳过真实辅助功能、Finder、硬件与常驻 Helper 依赖。
- 在 CI 运行设置导航、模块列表和权限页 smoke；签名与权限路径继续由 T012 验收。

## Acceptance Criteria

- 本机 ad-hoc UI test 与 GitHub Actions job 都通过，并保留 XCTest 失败日志。
- 不降低 Core 或完整构建门禁。

## Validation

- 运行设置导航 UI test 和 GitHub Actions job，保存 XCTest 结果。

## Evidence

- 已有 `--ui-testing` 启动模式；它以 regular activation 运行并跳过全局热键、特权命令 observer 与展示模式恢复。
- 2026-08-25 本机 UI test 在 65 秒后失败：`Application 'com.number47.fewer' has not loaded accessibility`。
- 解除条件：在具备辅助功能授权的 GUI runner 上完成本机测试；在此之前不得接入阻断 CI job。
