# T033: 普通 CI UI smoke

## 元信息

- 优先级：P1
- 状态：BLOCKED
- 推荐执行器：implementer
- 依赖：T027

## Goal

让 GitHub Actions 运行无需 TCC 的稳定 UI smoke，尽早发现设置与模块导航回归。

## Requirements

- 新增可重复 UI-test 启动模式，禁止依赖真实辅助功能、Finder、硬件或常驻 Helper。
- CI 阻断运行设置导航、模块列表和权限页 smoke；签名/权限/菜单栏路径继续留给 T012。
- 失败日志必须保留 XCTest 结果，不降低 Core/full-build 门禁。

## Validation

- 本机 ad-hoc UI test 与 GitHub Actions job 均通过。

## Blocker Evidence

- 已新增 `--ui-testing` 启动模式：UI 测试以 regular activation 运行，并跳过全局热键、特权命令 observer 与展示模式恢复。
- 2026-08-25 本机 ad-hoc `xcodebuild test -only-testing:FewerUITests/FewerUITests/testPermissionsSettingsPageNavigation` 在 65 秒后失败：`Application 'com.number47.fewer' has not loaded accessibility`。
- 当前 XCTest UI host 未获辅助功能授权；在具备授权 GUI runner 前不得把该命令接入阻断 CI job。
