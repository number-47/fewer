# T027: CI Swift 6 修复

## 元信息

- 优先级：P0
- 状态：DONE
- 推荐执行器：implementer
- 依赖：无

## Goal

恢复 `main` 的完整 Fewer Debug build：将 AppKit 绘制专用 `MarkupRenderer` 隔离到 `@MainActor`，消除共享 `CIContext` 的 Swift 6 strict-concurrency error。

## Requirements

- 不使用 `@preconcurrency import CoreImage`、`@unchecked Sendable` 或 `nonisolated(unsafe)` 压制诊断。
- 保留现有共享 `CIContext` 与渲染行为；不做 Markup 大文件拆分。
- 以完整 Fewer build 作为回归门禁，并核对 GitHub Actions 对应提交为绿色。

## Validation

```bash
./script/ci_verify.sh
gh run list --commit HEAD --limit 1
```

## Exclusions

- XPC、Finder、Store、截图内存和 UI 改动。

## Completion Report

- `MarkupRenderer` 已整体标记为 `@MainActor`，共享 `CIContext` 保持在 AppKit 绘制隔离域。
- 未使用 `@preconcurrency`、`@unchecked Sendable` 或 `nonisolated(unsafe)`。
- 2026-08-25 本机通过 `./script/ci_verify.sh`（338 Core tests、完整 Debug build、模板、三产物）和 `./script/verify.sh`。
