# T054: OCR 翻译浮窗外部点击关闭与固定

## Metadata
- Priority: P1
- Status: BLOCKED
- Dependencies: T046

## Goal

让 OCR 翻译结果浮窗默认在用户点击窗外后自动关闭，并允许用户固定窗口以持续置顶查看。

## Scope

- 修改 OCR 翻译窗口控制器、视图状态与必要测试。
- 默认未固定；点击其他窗口、桌面或其他应用后关闭，并沿用现有 dismiss 清理 OCR 文本和翻译任务。
- 浮窗提供明确的固定/取消固定按钮；固定后继续置顶且失去焦点不关闭，取消固定后恢复自动关闭。
- 新一轮 OCR 截图仍替换当前结果窗口；固定状态不持久化、不允许保留多份 OCR 文本。
- 不修改 OCR、Apple Translation、AI 请求、语言选择、窗口位置设置或普通截图贴图流程。
- 由窗口控制器持有固定状态，SwiftUI 仅展示状态并通过回调请求切换；不得继续以 ViewModel 作为失焦关闭判断的唯一事实源。

## Acceptance Criteria

- 新 OCR 结果窗默认未固定，点击窗外后关闭并清理当前会话。
- 固定按钮状态清晰可见；固定时点击窗外不关闭，再次取消固定后下一次窗外点击关闭。
- 点击浮窗内部的复制、翻译源、目标语言、重试、设置和固定按钮不会被当作窗外点击。
- 手动关闭和新 OCR 请求继续执行现有清理/替换逻辑；固定状态不写入任何持久化存储。
- 固定后切换到桌面、其他窗口、其他应用或 Space 时继续显示；取消固定后下一次失焦恢复关闭。
- Fewer Debug 构建、`git diff --check` 与 macOS 26 签名交互矩阵通过。

## Validation

- Fewer Debug 无签名构建。
- `python3 script/verify_agent_state.py`
- `git diff --check`
- macOS 26 签名运行中验证：窗内各控件、点击其他 Fewer 窗口、桌面和其他应用；固定、取消固定、手动关闭及连续 OCR。

## Evidence

- 2026-09-03：任务建立前工作区干净，`main` 比 `origin/main` 超前 1 个本地提交。现有 `OCRTranslationPanel` 已为 `.floating` 且 `hidesOnDeactivate = false`，但没有失焦关闭处理或固定状态；控制器关闭时会清空 `OCRTranslationViewModel` 并通知协调器取消会话。
- 2026-09-03：采用最小窗口策略：由当前窗口 ViewModel 持有不持久化的固定状态；`NSWindowDelegate.windowDidResignKey` 只在未固定时走现有关闭/清理路径。固定不改变窗口层级，只控制失焦是否自动关闭。
- 2026-09-03：`OCRTranslationViewModel` 已增加默认关闭的内存固定状态、切换方法及清理重置；原文标题行增加图钉按钮，并提供“固定浮窗/取消固定浮窗”的帮助与辅助功能标签。
- 2026-09-03：结果面板失去 key 后会异步复核面板身份、固定状态和是否重新成为 key；只有当前面板仍未固定且仍处于失焦状态时，才调用既有 `closeResultWindow(notify: true)`，因此继续清空 OCR 文本并取消当前翻译会话。固定后失焦保持，取消固定后下一次失焦恢复关闭；新 OCR 请求仍替换旧窗口。
- 2026-09-03：主对话审查确认仅修改 OCR 窗口控制器与视图，未改变窗口尺寸/位置、OCR、语言选择、Apple Translation、AI 请求、普通截图或持久化。`git diff --check` 和静态窗口交互接线检查通过，未发现范围内 P0/P1/P2 问题。
- 2026-09-03：当前 WSL 环境没有 `xcodebuild`、`xcbeautify`、`xcodegen` 或 `swift`，无法执行 Fewer Debug 构建和 macOS 26 真实窗口交互。`python3 script/verify_agent_state.py` 仍被远程已有的重复任务 ID `T050` 阻断，本任务未修改该范围外状态。
- 2026-09-04：用户实机反馈点击图钉后窗口没有固定展示，说明现有“ViewModel 状态 + 失焦时读取”的实现未满足验收。任务重新进入实施；修复应由窗口控制器同步拥有固定状态，并在固定时明确维持浮动层级、失活可见和置前行为。
- 2026-09-04：窗口控制器现持有当前结果窗唯一的 `isPinned` 状态；图钉按钮仅发送切换请求，控制器同步更新 ViewModel 展示。固定时明确保持失活可见、按普通/截图抑制状态选择浮动层级并 `orderFrontRegardless()`；失焦关闭只读取控制器固定状态，释放和新会话均重置。主对话审查未发现范围内 P0/P1/P2 问题，且未修改高度、OCR、翻译、截图或持久化逻辑。
- 2026-09-04：`git diff --check -- FewerApp/Views/OCRTranslationWindowController.swift FewerApp/Views/OCRTranslationView.swift` 通过。当前 WSL 没有 Xcode/Swift 工具链，不能执行 Debug 构建和真实失焦/Space 交互；代码实现不再阻塞 T056，但 T054 本身仍等待实机验收。

解除条件：在 macOS 26 签名运行中验证未固定时点击其他 Fewer 窗口、桌面和其他应用均关闭；固定后在上述场景及切换 Space 时均保持；取消固定后恢复关闭；同时验证复制、翻译源、目标语言、重试、设置、手动关闭和连续 OCR。
