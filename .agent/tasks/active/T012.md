# T012: 集成与发布验收

## 元信息

- 优先级：P0
- 状态：BACKLOG
- 推荐执行器：Sol
- 依赖：T017, T029, T030, T031, T032, T033, T034, T035

## Goal

在所有发布相关修改完成后执行最终全量验证、DMG 打包、签名/Runtime 检查和实机验收；只有此任务通过才可声明发布完成。

## 验证

- `./script/verify.sh`
- `./script/package.sh --local`
- 对 DMG、嵌套 Helper/Extension 签名和 Hardened Runtime 检查。
- 实机核对五个状态项、日历、按键展示和右键手势；网络覆盖默认 VPN/非默认 VPN，GPU 覆盖可用与不可用字段。
- 按 T026 的矩阵核对 Finder/Chrome/Safari 普通右键、手势不弹菜单、Finder 扩展菜单、全部权限入口、设置窗口和日历连续滚动。
- 验证签名 XPC 正反向、伪客户端拒绝、三进程同一 App Group container、无 `/` Finder entitlement、跨进程 Store 并发与损坏恢复、滚动截图峰值内存和普通 CI UI smoke。
