# T012: 集成与发布验收

## Metadata

- Priority: P0
- Status: PENDING
- Dependencies: T017, T029, T030, T031, T032, T033, T034, T035, T036, T037, T038

## Goal

在全部发布相关任务完成后完成全量验证、DMG 打包、签名/Runtime 检查和签名实机验收；只有通过后才能声明发布完成。

## Scope

- 验收全部发布相关修改、嵌套 App 签名、App Group、Finder 权限和跨进程行为。
- 不在本任务顺手实现产品修复；发现问题时保留证据并创建或恢复对应任务。

## Acceptance Criteria

- `verify.sh`、本机 DMG、嵌套 Helper/Extension 签名和 Hardened Runtime 检查通过。
- 完成状态项、日历、按键展示、右键手势、Finder、权限、设置和连续滚动的签名实机矩阵。
- 验证双向 XPC、伪客户端拒绝、三进程共享容器、无 Finder 根路径例外、Store 恢复和并发、截图内存限制与普通 UI smoke。

## Validation

- `./script/verify.sh`
- `./script/package.sh --local`
- 对 DMG 和嵌套 Helper/Extension 运行签名与 Hardened Runtime 检查，并记录实机矩阵。

## Evidence

尚未开始；历史代码层和未完成实机证据保留在归档任务中。
